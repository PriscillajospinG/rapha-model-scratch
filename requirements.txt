# Fallback for `pip install -r requirements.txt`. The primary install path
# is `pip install -e .` (pyproject.toml), which also registers the
# rehab-* console script commands documented in README.md.
yt-dlp
opencv-python<4.10
opencv-contrib-python<4.10
ultralytics
imagehash
pandas
numpy<2
Pillow
torch
torchvision
torchaudio
mediapipe
scikit-learn
matplotlib
onnx
onnxruntime
scipy
flask
cryptography
